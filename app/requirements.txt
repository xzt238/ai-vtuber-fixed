# GuguGaga AI VTuber - Dependencies
# ===================================
# Install via: install_deps.bat
# Or manually: pip install -r requirements.txt
#
# Python 3.11 required
# CUDA 12.1 recommended for GPU features
#
# NOTE: torch/torchaudio/torchvision 的 CUDA 版本由 install_deps.bat 安装
#       不要在这里锁定 torch 版本，否则 pip 可能降级为 CPU 版

# ===== Core (required) =====
pyyaml>=6.0
requests>=2.28.0
edge-tts>=7.0
websocket-server>=0.6
websockets>=10.0

# ===== Audio/Image Processing (required) =====
numpy>=1.24.0,<2.0.0
pillow>=10.0
soundfile

# ===== ASR - Speech Recognition (recommended) =====
faster-whisper>=1.0
funasr>=1.0
sounddevice>=0.4
mss>=10.0

# ===== Chinese NLP (required) =====
jieba>=0.42

# ===== ML Frameworks (required for GPT-SoVITS / MiniCPM) =====
# torch/torchaudio 由 install_deps.bat 通过阿里云镜像安装 CUDA 版
# 这里仅声明依赖关系，不锁定版本
torch
torchaudio
transformers>=4.44.0,<4.45.0
peft>=0.10.0
accelerate>=0.20.0

# ===== Vision / Screen Analysis (optional) =====
opencv-python>=4.8
rapidocr-onnxruntime>=1.3.0
modelscope>=1.9.0

# ===== Memory System - Optional (RAG / Vector DB) =====
# sentence-transformers 仅在使用向量嵌入记忆时需要，有 fallback 机制
# sentence-transformers>=2.0

# ===== Build / Packaging Tools (NOT needed at runtime) =====
# Move to requirements-build.txt if needed:
# pyinstaller>=6.0
pywin32>=306; sys_platform == 'win32'
